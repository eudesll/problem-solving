/* 
   URI
   Problem Id : 1006
   Problem Name: Média 2
*/

#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
 
int main () {
    double a, b;
    cin >> a >> b;
    cout << "MEDIA = " << setiosflags (ios::fixed) << setprecision (1) << ((a * 2) + (b * 3) + (c * 5))/10 << endl; 
 
    return 0;
}